[project]
name = "precipitate"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Matthew Mahowald", email = "matthew.mahowald@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "onnxruntime>=1.23.2",
    "pydantic>=2.12.5",
    "pytest>=9.0.2",
    "torch>=2.10.0",
    "transformers>=5.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pyright>=1.1.408",
    "pytest>=9.0.2",
    "ruff>=0.14.14",
]

[tool.ruff]
fix = true

[tool.ruff.lint]
extend-select = ["I", "ANN", "RUF", "ASYNC", "PTH", "SIM", "FURB"]
ignore = ["ANN003", "ANN002"]
